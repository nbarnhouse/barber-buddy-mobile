import { registerRootComponent } from 'expo'; // Handles app lifecycle and registration
import App from './App'; // Main application component

// Register the root component of the app
registerRootComponent(App);
